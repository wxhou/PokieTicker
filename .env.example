# ── AI ──────────────────────────────────────────────────────
# MiniMax API key (required for AI features)
# Sign up at: https://platform.minimaxi.com/
# Uses MiniMax M2.5 via Anthropic-compatible endpoint
MINIMAX_API_KEY=your_minimax_api_key_here

# ── Auth ─────────────────────────────────────────────────────
# JWT secret for user authentication (change in production!)
JWT_SECRET=dev-secret-change-in-production

# ── China Data ────────────────────────────────────────────────
# Tushare token (optional, for additional A-share data)
TUSHARE_TOKEN=